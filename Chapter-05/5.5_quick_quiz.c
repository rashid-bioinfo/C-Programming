/*
Quick Quiz: Use the library functions to calculate the are of a square with side a
*/

#include<stdio.h>
#include<math.h>

int area(int side){
    return side*side;
}

int main(){
    int a = 0;

    a = area(9);

    printf("This is main function. \n");
    printf("area of square is: %d. \n", a);
    printf("area of square is: %d. \n", a);
    printf("area of square is: %d. \n", a);

    return 0;
}


int main(){
    int a = 0;
    a = area(9);
    printf("This is the main function")
}